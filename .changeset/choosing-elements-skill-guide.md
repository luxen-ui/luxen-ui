---
'luxen-ui': minor
---

The generated agent skill now ships a `choosing-elements.md` reference that helps AI agents pick the right element from user intent — when to prefer a native HTML element with a CSS class over a custom element, and how to tell apart common look-alikes (alert vs toast, switch vs checkbox, dropdown vs select).
