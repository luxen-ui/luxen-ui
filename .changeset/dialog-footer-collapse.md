---
'luxen-ui': patch
---

Dialog and drawer no longer reserve empty space for the footer when no `footer` slot content is provided — the footer row now collapses to zero height instead of showing a blank padded band.
