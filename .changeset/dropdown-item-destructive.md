---
'luxen-ui': patch
---

Add `variant="destructive"` to `<l-dropdown-item>`. Set it to mark a delete-style menu action in red, with a danger-tinted highlight on hover and focus. The item still selects and fires the dropdown's `select` event like any other.
