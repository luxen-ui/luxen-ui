---
'luxen-ui': patch
---

Add `--header-padding` and `--footer-padding` custom properties to `<l-dialog>`, both defaulting to `--padding`, so the header and footer paddings can be tweaked independently.
