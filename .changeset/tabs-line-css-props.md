---
'luxen-ui': patch
---

`<l-tabs variant="line">` now exposes four CSS custom properties to restyle the
indicator and track: `--indicator-color`, `--indicator-thickness`,
`--track-color` and `--track-thickness`.
