---
'luxen-ui': patch
---

Fix the `<l-prose-editor>` emoji picker not appearing (and being unclickable) when the editor is used inside a modal `<l-dialog>`. The picker now opens in the top layer and stays interactive within the dialog.
