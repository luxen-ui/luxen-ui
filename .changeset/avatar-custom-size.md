---
'luxen-ui': patch
---

Add a public `--size` custom property to `<l-avatar>` for arbitrary pixel sizing. Set `--size` to any length to render an avatar at a custom size beyond the `xs`–`xl` token scale; the font scales proportionally and the radius (rounded square or circle) follows. The named `size` tokens are unchanged.
