---
'luxen-ui': minor
---

Add `data-marker-placement` to `<l-disclosure>`. Set `data-marker-placement="start"` to render the marker icon before the summary label instead of after it (the default remains `end`). Works with both `arrow` and `plus` markers.
