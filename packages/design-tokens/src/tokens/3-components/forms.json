{
  "$description": "Form component tokens — shared by every form control (checkbox, radio, switch, input, textarea, select) and the `l-form-field` wrapper. Two families: `form.control.*` (control appearance) and `form.field.*` (field layout). References semantic aliases so theming/dark-mode is inherited.",
  "form": {
    "control": {
      "activated-color": {
        "$type": "color",
        "$value": "{color.blue.600}",
        "$description": "Color of a control when activated (checked checkbox, selected radio, on switch). Form-wide accent, akin to native `accent-color`. A stable, saturated color that does not invert between light and dark, so the baked white glyph stays legible in both modes — override per brand."
      },
      "activated-content-color": {
        "$type": "color",
        "$value": "{color.white}",
        "$description": "Color of the glyph on the activated fill (checkmark, radio dot, switch thumb). Pairs with the stable accent."
      },
      "background-color": {
        "$type": "color",
        "$value": "{color.surface}",
        "$description": "Control background (input, textarea, select, unchecked toggle box)."
      },
      "value-color": {
        "$type": "color",
        "$value": "{color.text.primary}",
        "$description": "Color of the user-entered value text."
      },
      "placeholder-color": {
        "$type": "color",
        "$value": "{color.text.tertiary}",
        "$description": "Placeholder text color."
      },
      "border-color": {
        "$type": "color",
        "$value": "{color.border}",
        "$description": "Control border color."
      },
      "border-width": {
        "$type": "dimension",
        "$value": "1px",
        "$description": "Control border width."
      },
      "border-radius": {
        "$type": "dimension",
        "$value": "{radius-md}",
        "$description": "Control border radius."
      },
      "border-color-hover": {
        "$type": "color",
        "$value": "{color.text.tertiary}",
        "$description": "Control border color on hover."
      },
      "border-color-invalid": {
        "$type": "color",
        "$value": "{color.bg.fill.danger.strong}",
        "$description": "Control border color when invalid."
      },
      "disabled-background": {
        "$type": "color",
        "$value": "{color.bg.disabled}",
        "$description": "Background of a disabled text control — a subtle grey fill (reinforces non-interactivity, instead of fading the whole control with opacity)."
      },
      "disabled-border": {
        "$type": "color",
        "$value": "{color.border-disabled}",
        "$description": "Border color of a disabled text control."
      },
      "disabled-color": {
        "$type": "color",
        "$value": "{color.text.disabled}",
        "$description": "Value/placeholder text color of a disabled text control."
      },
      "toggle-size": {
        "$type": "dimension",
        "$value": "1.25em",
        "$description": "Box size of toggle controls (checkbox, radio, switch). Relative to font size so it scales with the label."
      },
      "toggle-halo-ratio": {
        "$type": "number",
        "$value": "0.22",
        "$description": "Thickness of the hover halo ring on toggle controls (checkbox, radio, switch), as a fraction of the toggle size. Single source for the shared toggle hover language."
      },
      "track-off-color": {
        "$type": "color",
        "$value": "{color.gray.500}",
        "$description": "Off-state track fill of the switch. A mid grey that holds in both light and dark so the white thumb keeps ~5:1 contrast either way — decoupled from text tokens so re-theming body text never moves the track."
      },
      "height": {
        "$type": "dimension",
        "$value": "{size.control.md}",
        "$description": "Height of text controls (input, select, textarea)."
      },
      "padding-inline": {
        "$type": "dimension",
        "$value": "{spacing-3}",
        "$description": "Horizontal padding of text controls."
      },
      "label-color": {
        "$type": "color",
        "$value": "{color.text.primary}",
        "$description": "Field label color."
      },
      "hint-color": {
        "$type": "color",
        "$value": "{color.text.secondary}",
        "$description": "Helper/description text color (`l-hint`)."
      },
      "error-color": {
        "$type": "color",
        "$value": "{color.text.danger}",
        "$description": "Error message text color (`l-error`)."
      },
      "required-content": {
        "$type": "content",
        "$value": "'*'",
        "$description": "Marker appended to a required field's label."
      },
      "required-color": {
        "$type": "color",
        "$value": "{color.text.danger}",
        "$description": "Color of the required marker."
      }
    },
    "field": {
      "gap": {
        "$type": "dimension",
        "$value": "{spacing-1}",
        "$description": "Vertical gap between label, control, and messages in a stacked field. Tight, so the label and helper text sit close to the control."
      },
      "choice-gap": {
        "$type": "dimension",
        "$value": "{spacing-2}",
        "$description": "Horizontal gap between a toggle control and its label in an inline field."
      }
    }
  }
}
