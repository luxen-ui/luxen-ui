/*
 * Metadata sidecar for the native <kbd> element (CSS-only, styled via `.l-kbd`).
 * JSDoc-only — read by the `native-meta` CEM plugin and emitted into
 * dist/metadata/kbd.json by normalize-metadata.mjs. Keep in sync with
 * src/css/elements/kbd.css.
 */

/**
 * @summary Displays keyboard keys or shortcuts inline with text.
 *
 * @nativeElement kbd
 * @selector .l-kbd
 *
 * @cssClass .l-kbd - Base class for keyboard key styling on `<kbd>` elements.
 *
 * @example Single key
 * <kbd class="l-kbd">Esc</kbd>
 */
// oxlint-disable-next-line typescript/no-extraneous-class -- JSDoc-only metadata carrier.
export class KbdMeta {}
